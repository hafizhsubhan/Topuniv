<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.sch.smktelkom_mlg.project.xiirpl509192939.topuniv">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".home"
            android:label="Topuniv"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".pilihuniv"
            android:label="Pilih Universitas" />
        <activity android:name=".search_univ" />
        <activity
            android:name=".detail_univ"
            android:label="@string/title_activity_detail_univ"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>